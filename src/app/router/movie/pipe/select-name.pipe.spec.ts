import { SelectNamePipe } from './select-name.pipe';

describe('SelectNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectNamePipe();
    expect(pipe).toBeTruthy();
  });
});
